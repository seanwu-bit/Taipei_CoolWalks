# 📘 [交接文件] 靜態綠意與基礎路網資料庫 (Handoff Documentation)

本文件由「靜態綠意與基礎路網工程師」產製，旨在說明大安區行人綠意路網的處理邏輯、數據定義與檔案規範，以利後續演算法開發與視覺化呈現。

---

### 1. 📂 產出檔案清單與適用情境

為利於跨模組協作，所有資料皆統一主檔名，僅以副檔名 (`.geojson`, `.csv`, `.gpkg`) 區分格式。提供兩種結構版本：

| 核心資料集名稱 (Base Name) | 適用團隊 | 資料結構與用途說明 |
| :--- | :--- | :--- |
| **`Daan_Shaded_Network(分段)_v1`** | 視覺化組<br>微氣候分析 | **微觀層次 (50m細分)**：用於精確捕捉街道熱點與熱島效應。此版本保留了最精密的遮蔭變化，適合繪製熱力圖與前端展示。 |
| **`Daan_Shaded_Network(還原)_v1`** | 演算法組<br>路徑導航 | **宏觀層次 (OSM拓樸)**：已將分數聚合回原始路口節點 (Intersection to Intersection)。大幅減少 Graph 節點數，**強烈推薦**用於 `NetworkX` 最短路徑/最涼爽路徑計算。 |

*(💡 註：演算法組建議直接讀取 `(還原)_v1.csv` 建立 Edge weights，速度最快；GIS 視覺化組請讀取 `.geojson` 或 `.gpkg` 以保留空間圖形。)*

---

### 2. 🛣️ 基礎路網擷取與過濾邏輯

為專注於**「人行環境」**，路網基底經過以下嚴格篩選：
* **🚫 排除車本幹道**：剔除 `motorway`, `trunk`, `primary`, `secondary` 及其聯絡匝道。
* **✅ 保留人行空間**：保留 `tertiary`, `residential`, `living_street`, `pedestrian`, `footway`, `path`, `steps`, `cycleway`。
* **✂️ 幾何細分 (Densification)**：計算前，所有路段皆切分為**最大 50 公尺**。確保能精準捕捉「路口」與「林蔭中段」的微氣候差異，避免長路段平均值的失真。（此動作於「分段版」保留，於「還原版」聚合）。

---

### 3. 📊 核心權重與欄位定義 (Data Dictionary)

請直接調用以下核心指標（數值皆已處理完畢）：

| 欄位名稱 | 定義 | 數值範圍 | 計算邏輯說明 |
| :--- | :--- | :--- | :--- |
| **`shading_index`** | **核心遮蔭指數** | **0.0 - 1.0** | `(NDVI_norm * 0.6) + (Tree_Density_norm * 0.4)`。<br>數值越高代表遮蔽越佳、體感越涼爽。 |
| **`safety_score`** | **行人安全係數** | **0.3 - 1.0** | 根據 OSM `highway` 分級賦予的物理安全分。<br>步道(1.0) > 生活巷弄(0.9) > 住宅區(0.7) > 混合道路(0.5)。 |
| `ndvi_mean` | 夏季綠意均值 | -0.2 - 0.8 | 2025 年夏季 Sentinel-2 影像實測值，已扣除 ESA Offset 進行真實反射率校正。 |
| `tree_count` | 實體樹木量 | 整數 (≥0) | 路段緩衝區內包含的臺北市政府登記行道樹棵數。 |
| `length` | 實際長度 | 公尺 (m) | 該路段在實體世界中的幾何長度。 |

---

### 4. 💡 給演算法同學的開發建議 (Tips for Success)

1. **權重獨立性 (Multi-Objective)**：`shading_index` 與 `safety_score` 是**互相獨立**的欄位。建議在路徑規劃演算法中，讓使用者自訂兩者的權重佔比。
2. **免自行正規化**：所有 Index 皆已完成 `MinMaxScaler` 處理。可直接轉化為成本函數（Cost Function，例如：`Cost = Length / Shading_Index`，讓分數越高的路段 Cost 越小）。
3. **座標系統**：提供之空間檔案預設為地圖通用的 `EPSG:4326` (WGS84 經緯度)。